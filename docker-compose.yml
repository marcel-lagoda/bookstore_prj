  version: '3.7'
    
  services:
    db:
      image: postgres:11
      environment:
        - POSTGRES_DB=postgres
        - POSTGRES_USER=postgres
        - POSTGRES_PASSWORD=postgres
    web:
      build: .
      command: python manage.py runserver 0.0.0.0:8000
      environment:
        - SECRET_KEY=6@q@_rz3k99^l%(z&@t)6ns4t(bk*9!g8iu_6!&&0+e-1645-e
        - DEBUG=1
      volumes:
        - .:/code
      ports:
        - "8000:8000"
      depends_on:
        - db
